<?php
  function contact_custom_form_alter(&$form, &$form_state, $form_id) {
    if($form_id == 'contact_site_form');
      $form['copy']['#access'] = 0;
      $form['copy']['#default_value'] =0;
      $form['subject']['#default_value'] = 'Feed me with Feedback';
      // print '<pre>';
      // print_r($form);
      // print '</pre>';
      // exit;

  }
